import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  footer: {
    fontWeight: 'bold',
    fontSize: 20,
    textAlign: 'center',
    color: 'black',
  },
});

export default styles;